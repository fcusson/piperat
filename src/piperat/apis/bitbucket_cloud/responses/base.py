"""Base Response typed dictionary from the bitbucket cloud api."""

from typing import TypedDict


class BitbucketCloudResponse(TypedDict):
    """Base Response typed dictionary from the bitbucket cloud api."""

    type: str
